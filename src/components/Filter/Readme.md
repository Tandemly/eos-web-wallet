Fitler list example:

```js

<Background className="filter-search-example content">
  <DisplayRedux>
    <DisplayReactRouter>
      <Filter data={transactions.default.slice(0, 7)}>
        <Transactions />
      </Filter>
    </DisplayReactRouter>
  </DisplayRedux>
</Background>
```
