Reset Password form example:

```js
const noop = () => {};

<div className="header-example">
  <DisplayRedux>
    <DisplayReactRouter>
      <ErrorBoundary>
        <ResetPasswordForm callAPI={noop} />
      </ErrorBoundary>
    </DisplayReactRouter>
  </DisplayRedux>
</div>
```
